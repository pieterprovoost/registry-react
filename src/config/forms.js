
const comment = require('./forms/comment')
const contact = require('./forms/contact')
const dataset = require('./forms/dataset')
const endpoint = require('./forms/endpoint')
const identifier = require('./forms/identifier')
const installation = require('./forms/installation')
const machineTag = require('./forms/machineTag')
const node = require('./forms/node')
const organization = require('./forms/organization')
const tag = require('./forms/tag')
const user = require('./forms/user')


module.exports = {
    comment: comment,
    contact: contact,
    dataset: dataset,
    endpoint: endpoint,
    identifier: identifier,
    installation: installation,
    machineTag: machineTag,
    node: node,
    organization:organization,
    tag: tag,
    user: user
}